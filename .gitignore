# deps / builds
node_modules/
dist/

# local state & data
.wrangler/
out/
*.log

# secrets — never commit
.env
.env.*
.dev.vars*
!.dev.vars.example
# MCP registry domain-auth private key (pairs with the public key served at
# refd.ai/.well-known/mcp-registry-auth). Needed to publish server.json.
.mcp-registry-key.hex
# committed public (non-secret) build-time origins, per Vite/Astro mode
!apps/web/.env.development
!apps/web/.env.production
!apps/dashboard/.env.development
!apps/dashboard/.env.production

# generated
worker-configuration.d.ts
scripts/.*.tmp.sql
vite.config.ts.timestamp-*
.astro/

# internal planning docs — kept locally, never published
/GTM.md
/docs/plan-agent-native.md
/docs/plan-three-worker.md

# os / editor noise
.DS_Store
