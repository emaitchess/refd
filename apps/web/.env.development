# Public build-time config for `astro dev` (local). Non-secret (baked into the
# client). The dashboard app the landing links to, on the local same-site host.
PUBLIC_DASHBOARD_ORIGIN=https://dash.refdlocal.io
