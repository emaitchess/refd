{
  "name": "@refd/dashboard",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@refd/core": "workspace:*"
  },
  "scripts": {
    "check": "tsc --noEmit",
    "test": "bun test",
    "lint": "biome check .",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "wrangler deploy"
  }
}
